venv/
__pycache__/
*.pyc
*.pkl
data.csv
hand_landmarker.task
.DS_Store

# Secrets — JANGAN commit. Key asli taruh di .env (lokal) & Vercel env vars.
.env
.env*.local
.vercel

# ─── Windows launcher scripts (lokal, jangan masuk GitHub) ───
*.bat
*.vbs

# ─── Docs = konteks pribadi (DESIGN/CHANGELOG/brief) — jangan ke GitHub ───
docs/

# Backup aset asli sebelum di-resize (lokal aja)
assets/**/_originals/

# ─── ML training: file berat & aset lama (lokal aja, web nggak butuh) ───
training/stickers/          # stiker "shark cat" lama, udah diganti assets/stickers/
training/model_info.json
